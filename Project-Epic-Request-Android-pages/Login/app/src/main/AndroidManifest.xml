<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FragmentRequestPage"
            android:label="@string/title_activity_fragment_request_page"
            android:theme="@style/AppTheme.NoActionBar"
            android:configChanges="orientation|screenSize"></activity>
        <activity
            android:name=".RequisitionForm2"
            android:label="@string/title_activity_requisition_form2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RequisitionActivity"
            android:label="@string/title_activity_requisition"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RecyclerViewJson" />
        <activity android:name=".MyRecycleView" /> <!-- <activity android:name=".RecyclerView" /> -->
        <activity
            android:name=".BurgerMenu"
            android:colorMode="@color/cardview_dark_background"
            android:icon="@color/cardview_dark_background"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgotPassword" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".RequestListPageFragment" />-->
    </application>

</manifest>